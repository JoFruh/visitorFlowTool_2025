
#function choosePath
#based on agent type and given nodes and path characteristics,
#determine which path is chosen

#inputs:
#agentType = string ("walker", "jogger" etc.)



choosePath <- function(agentType, paths){

  #paths example data:
  # c( c(attr: 5, ),  c())

  finalPath <- NULL

  #use agentType to reference weights in dataframe







  #combine all probabilities for every path

  #choose based on probabilities
  #finalPath <- sample(pathChoices, 1, prob = pathProbs)

  return(finalPath)

}
